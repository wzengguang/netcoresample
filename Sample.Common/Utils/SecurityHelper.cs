using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Cryptography;
using System.Text;
using System.Threading.Tasks;

namespace Sample.Common.Utils
{
    public class SecurityHelper
    {
        public static string AesEncrypt(string text, string key, bool autoHandle = true)
        {
            //待未实现
            return text;
        }

        public static string AesDecrypt(string text, string key, bool autoHandle = true)
        {
            //待未实现
            return text;
        }
    }
}
